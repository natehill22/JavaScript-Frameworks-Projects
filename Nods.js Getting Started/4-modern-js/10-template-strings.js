const greeting = "Hello World";

const answer = 'Forty Two';

//Template strings (backticks) are used for multi-line strings and also inject JS into itself
const html = `
  <div>
    ${Math.random()}
  </div>
`;
