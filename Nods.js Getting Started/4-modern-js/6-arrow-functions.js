const square = (a) => {
  return a * a;
};

// const square = (a) => a * a;
// const square = a => a * a;

[1, 2, 3, 4].map(a => a * a);

//All three declarations perform exact same math operation, but use different level of syntactic sugar
