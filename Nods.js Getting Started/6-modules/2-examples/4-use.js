const templateGenerator = require('./4-function'); //Importing previous module (4-function)

const myTemplate = templateGenerator('Hello Node!'); //Writes the html in the previous module to (and specified argument) to a new variable

console.log(myTemplate); //Printing data from specified module

